//Print the first 10 natural numbers. 

#include <iostream>

int main() {

    std::cout << "Numbers: ";
    for (int i = 1; i <= 10; i++) { 
        std::cout << i << std::endl; //print i, (the natural number) during every iteration in a new line
    }

return 0;
}